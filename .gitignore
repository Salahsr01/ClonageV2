node_modules/
dist/
dist-test/
# Data dirs — may exist as real directories OR as symlinks to an external
# drive. Match both forms (no trailing slash ⇒ matches symlinks too).
/output
/generated
/.clonage-kb
.playwright-mcp/
.DS_Store
*.log
# screenshots de debug à la racine
/*.png
.env
.env.local
.env.*.local
